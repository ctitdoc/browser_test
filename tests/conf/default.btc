<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE json [
        <!--
        For a test with firefox browser : set in this entity your firefox profile directory,
        example : <!ENTITY profile "/home/franck/.mozilla/firefox/hvea6ko0.default">
        to get the list of profiles used or usable by the local firefox config of your account:
        open firefox and run  the url : about:profiles
        or by defaul: a default profile should exist in a directory <home>/.mozilla/firefox/xxxxxx.default
        where xxxxxx is a random string.
        You can also have a look at this post : https://askubuntu.com/questions/239543/get-the-default-firefox-profile-directory-from-bash

        In the args array option below, add "-headless" to run tests without displaying the browser window.
        -->
        <!ENTITY profile "/home/franck/snap/firefox/common/.mozilla/firefox/qt5rc0hu.default-release">
        ]>
<json>
    {
      "browserName" : "firefox",
      "moz:firefoxOptions": {
    <!-- profile parameter should always be set as first parameter to ensure assert  on this parameter works -->
        "profile" : "&profile;",
        <!-- to switch between test with/without head(window) display, switch the "args" lines comment : -->
        <!-- "args": ["-headless"], -->
        "args": [],
        "log": {
          "level": "trace"
        }
      }
    }
</json>